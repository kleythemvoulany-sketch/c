rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to listing images
    // Allow authenticated users to write to the listings folder
    match /listings/{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
