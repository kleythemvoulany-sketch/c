rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Diagnostic rule: Allow all reads and writes temporarily.
      // This is not secure for production.
      allow read, write: if true;
    }
  }
}
