rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to images for listings
    // and only allow authenticated users to write (upload).
    match /listings/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
